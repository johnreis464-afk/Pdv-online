MONGODB_URI=mongodb://localhost:27017/pdv-system
# For MongoDB Atlas, use a connection string like the following (do NOT commit secrets):
# MONGODB_URI=mongodb+srv://<user>:<password>@cluster0.mongodb.net/pdv-system?retryWrites=true&w=majority
PORT=3000
